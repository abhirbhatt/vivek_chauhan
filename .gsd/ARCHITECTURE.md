# Architecture

> Auto-generated by /map on 2026-02-10

## Overview

Cinematic portfolio website for filmmaker Vivek Singh (AFilmCraft). The application is built using Next.js 16 with the App Router, leveraging high-performance animations (GSAP) and smooth scrolling (Lenis) to create an immersive user experience.

```
┌─────────────────────────────────────────┐
│               [Next.js App]             │
├─────────────────────────────────────────┤
│           [React Components]            │
│  (Hero, Mission, Gallery, Contact)      │
├─────────────────────────────────────────┤
│            [Animation Layer]            │
│       (GSAP, Lenis, Framer Motion)      │
└─────────────────────────────────────────┘
```

## Components

### Core Pages
- **Home (`src/app/page.tsx`)**: Main landing page composing the primary sections.
- **Layout (`src/app/layout.tsx`)**: Root layout defining fonts (Great Vibes, Bricolage Grotesque) and wrapping content in `SmoothScroll`.

### UI Components (`src/components/ui`)
- **Hero**: Landing section with video background, parallax effects, and signature animation.
- **Mission**: "About" section with marquee text reveal and noise texture background.
- **VideoGallery**: Showcase of video work.
- **Contact**: Contact form and information.
- **Footer**: Site footer.

### Legacy/Alternative Components (`components/`)
- **ParallaxHero.tsx**: An alternative or unused hero component.

## Data Flow

1. **Static Content**: Most content is hardcoded within React components for immediate rendering.
2. **User Interaction**:
    - **Scroll**: Triggers GSAP animations and Lenis smooth scrolling.
    - **Contact Form**: User input -> EmailJS SDK -> External Email Service.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| EmailJS | SDK | Handling contact form submissions (`@emailjs/browser`) |

## Technical Debt

- [ ] **Directory Structure**: split between `src/components` and root `components`. Recommend consolidating to `src/components`.
- [ ] **Hardcoded Content**: Text content is embedded in components, making updates harder for non-developers.

## Conventions

**Structure**: Next.js App Router (`src/app`).
**Styling**: Tailwind CSS with utility classes (`src/app/globals.css`).
**Animation**: GSAP for timeline-based animations, Lenis for scroll smoothing.
